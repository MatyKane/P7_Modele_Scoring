services:
  - type: web
    name: fastapi-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: PYTHONPATH=. uvicorn api.app:app --host=0.0.0.0 --port=8000
    runtime: python
    region: oregon
    branch: main
    autoDeploy: true