services:
  - type: web
    name: p7-api-scoring
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api.app:app --host=0.0.0.0 --port=10000"
    plan: free
    region: frankfurt